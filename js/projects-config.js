/**
 * Projects page settings (optional).
 *
 * CMS / WordPress:
 * - Usually you do NOT need this file.
 * - Render filters + project cards in HTML (see partials/projects-filters.html,
 *   partials/project-card.html) and keep data-projects-source="dom" on the section.
 * - pageSize can be set on the section: data-projects-page-size="6"
 */
window.ProjectsConfig = {
  pageSize: 6,
  pageSizeMobile: 4,
  allLabel: "All Projects",
};
